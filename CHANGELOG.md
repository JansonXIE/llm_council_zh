# 更新日志 (Changelog)

## [Unreleased]

### 新增
- **连续对话功能**：解除了聊天界面只能发送单次提问的限制。现在，在获得议会的回答后，您可以继续提问。
- **对话上下文支持**：支持了连续对话的记忆功能。每次发送新问题时，应用会自动在后台携带最近 3 轮的历史聊天记录（您的提问与主理人的最终总结）给各个大模型，让议会明白您刚刚聊了什么。
- 增加了在线更新功能。在设置中添加了“自动检查更新”的开关（默认开启），支持应用启动时自动在后台检测更新并弹窗提示。
- 配置了 GitHub Actions 以支持打包时自动对应用更新包进行数字签名，并生成 `latest.json` 用于在应用内检查版本更新。
- **对话管理**：新增了删除对话的功能，支持从本地存储中永久删除历史对话，并即时更新侧边栏列表。
- **模型管理功能**：在设置界面新增了模型管理，支持动态添加新的大模型配置、删除已有模型以及随时切换模型的启用/禁用状态。
- **多模型生态支持**：扩展了应用的模型接入范围，新增了对国内主流大模型（如 Kimi, DeepSeek, MiniMax, GLM 等）以及 Sophnet 大模型的原生配置支持。
- **本地应用基础建设**：使用 Tauri 和 Rust 构建了轻量级的桌面端应用架构，支持本地持久化保存用户的对话记录和自定义应用设置（API Keys、自定义模型等）。
- **权限与安全配置**：完善了应用的本地权限配置，确保内容写入（对话持久化和设置保存）能够安全进行。
